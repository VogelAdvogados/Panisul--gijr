{
  "name": "panisul",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "npm --workspace @panisul/backend run dev",
    "dev:frontend": "npm --workspace @panisul/frontend run dev",
    "build": "npm --workspace @panisul/backend run build && npm --workspace @panisul/frontend run build",
    "start": "npm --workspace @panisul/backend run start",
    "typecheck": "npm --workspace @panisul/backend run typecheck && npm --workspace @panisul/frontend run typecheck",
    "setup": "([ -f apps/backend/.env ] || cp apps/backend/.env.example apps/backend/.env 2>/dev/null || cp apps/backend/ENV.EXAMPLE apps/backend/.env) && npm run --workspace @panisul/backend prisma:generate && npm run --workspace @panisul/backend prisma:migrate && npm run --workspace @panisul/backend db:seed",
    "setup:dev": "npm run setup && npm run dev",
    "db:reset": "npm --workspace @panisul/backend run db:reset"
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "@typescript-eslint/eslint-plugin": "^8.40.0",
    "@typescript-eslint/parser": "^8.40.0",
    "concurrently": "^8.2.2",
    "eslint": "^9.33.0",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^5.2.0"
  }
}
